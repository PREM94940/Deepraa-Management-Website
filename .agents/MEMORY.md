# DEEPRASTORE: Long-Term Context Memory

> [!CAUTION]
> This file is automatically maintained by the `project-memory` skill. Do not manually delete sections without cause. This serves as the permanent brain of the AI infrastructure for this project.

## 1. Architectural Rules
*   **Framework:** Next.js 16.2.6 (App Router) + Supabase.
*   **Core Governance:** Must strictly adhere to the zero-hardcoding CMS policy and the "Adjustment-First" return policy.

## 2. AI System Directives (MANDATORY)
*   **Contextual Skill Suggestions:** At the end of EVERY implementation plan or major task completion, the AI MUST append a "Suggested Prompts & Skills" section. 
*   Because the user has many local skills and agents, the AI must explicitly recommend 1-2 relevant skills (from `.agents/skills/`) or subagents (from `AGENTS.md`) that fit the context of the current task.
*   The AI must provide copy-pasteable prompt examples for the user (e.g., "To verify this, you can run the `proactive-watcher` skill by typing...").

## 2. Known Pitfalls & Solutions
*   *(Memory engine initialized. Awaiting first autonomous report).*

## 3. Component Patterns
*   *(Memory engine initialized. Awaiting first autonomous report).*
